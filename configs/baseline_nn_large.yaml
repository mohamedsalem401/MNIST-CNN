method: nn_large
seed: 42
seeds: [11, 22, 33]
model:
  architecture: mlp
  hidden_sizes: [256, 128]
  dropout: 0.1
optim:
  epochs: 8
memory:
  enabled: false
logging:
  output_root: results
  experiment_name: baseline_nn_large
  save_checkpoints: false
